# My MCP Demo Server

This is a basic MCP server created for the MCP for Beginners assignment.

It includes:
- a tool: `add`
- a resource: `file://{path}`
- a prompt: `review_code`

## Run

```bash
pip install -r requirements.txt
python server.py